import React from "react";
import Svg, {  } from "react-native-svg";

const Error = props => (
<Svg width={props.width || 90} height={props.height || 112.5} viewBox="0 0 90 112.5"></Svg>
);

export default Error;
